[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    --strict-markers
    --strict-config
    -ra
markers =
    slow: takes more than a few seconds; deselect with -m "not slow"
    needs_torch: requires torch/torchvision/transformers
    needs_runtime: requires a compiled native runtime (the anytime_runtime extension or the subprocess worker)
filterwarnings =
    error::DeprecationWarning:anytime_serving.*
    default
