# Generated by scripts/profile_variants.py. Values are measured through the
# serving path on the host recorded in results/variant_profiles.json;
# re-run after changing hardware, thread count, or model variants.
deadline_ms: 38.7
workers: 4
duration_s: 20.0
arrival_rate_rps: 80.0
load_knee_percent: 50.0
load_slope: 0.02
admission:
  safety_factor: 1.0
variants:
  distilbert_fp32:
    service_time_ms: 12.893
    accuracy: 0.9106
    compute_cost_per_request: 1.0
  minilm_fp32:
    service_time_ms: 5.189
    accuracy: 0.9014
    compute_cost_per_request: 0.4025
